#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
cd "$SCRIPT_DIR/server"

export HOST="${HOST:-127.0.0.1}"
export PORT="${PORT:-18060}"

exec swift run Run
