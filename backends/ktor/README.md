# HelloTime Ktor 后端

Ktor 版本的 HelloTime 后端实现，采用 Kotlin + Ktor + SQLite。

## 运行

```bash
./run
```

Windows PowerShell:

```powershell
.\run.ps1
```

默认端口：`18090`

## 测试

```bash
./gradlew test
```

## 构建

```bash
./gradlew build
```
